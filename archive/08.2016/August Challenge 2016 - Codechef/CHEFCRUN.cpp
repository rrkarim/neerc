#include <bits/stdc++.h>
#define MAX_CHAR 256
#define MAXN 200005
#define INF 1000000
using namespace std;

typedef long long ll;
int n, x, y, s, f;
ll a[MAXN + 2]; int used[MAXN + 2];
int main() {
    
    int t; cin >> t;
    for(int i = 0; i < t; ++i) {
        scanf("%d", &n);
        for(int j = 0; j < n; ++j) scanf("%lld", a + i);
        scanf("%d%d", &s, &f);
        
    }

}
